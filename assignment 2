#include<stdio.h>
main()
{
	int n,i,r,count=0;
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		if(n%i==0)
		count++;
	}
	printf("count=%d\n",count,n);
	count=1;
	for(i=1;i<n;i++)
{
	if(n%i==0)
	{
		printf("[%d] -> %d\n",count,i);
		count++;
		}
	}
}
